> 本章译自 libuv 官方文档 guide/threads.rst（libuv 1.52）

# 线程与同步原语

等等，我们为什么突然讲起线程了？事件循环不是号称做 *web 规模编程* 的 **正道** 吗？呃……并不全是。处理器终究是在线程中执行工作的，因此线程有时依然大有用处——尽管你可能得趟过各种同步原语的泥潭。

libuv 在内部使用线程来模拟所有系统调用的异步特性。libuv 也借助线程让你（应用程序）能够异步地执行一个实际上是阻塞的任务：派生一个线程，并在任务完成时收集结果。

如今主流的线程库有两个：Windows 线程实现和 POSIX 的 `pthreads(7)`。libuv 的线程 API 与 pthreads API 类似，语义上也常常相近。

libuv 线程设施有一个值得注意的特点：它是 libuv 中一个完全自包含的部分。libuv 的其他功能都与事件循环和回调机制紧密耦合，而线程对这一切完全无感——它们可以按需阻塞，通过返回值直接报告错误，而且正如下面[第一个例子](#核心线程操作)所示，甚至不需要一个正在运行的事件循环。

由于各个平台上线程的语义和语法都各不相同、完善程度不一，libuv 的线程 API 也相当精简。

本章基于如下假设：**只有一个事件循环，它运行在一个线程（主线程）中**。其他任何线程都不与事件循环交互（除非通过 `uv_async_send`）。

## 核心线程操作

这里没什么复杂的东西：用 `uv_thread_create()` 启动一个线程，用 `uv_thread_join()` 等待它结束即可。

`thread-create/main.c`

```c
    int tracklen = 10;
    uv_thread_t hare_id;
    uv_thread_t tortoise_id;
    uv_thread_create(&hare_id, hare, &tracklen);
    uv_thread_create(&tortoise_id, tortoise, &tracklen);

    uv_thread_join(&hare_id);
    uv_thread_join(&tortoise_id);
    return 0;
}
```

> **提示**：`uv_thread_t` 在 Unix 上只是 `pthread_t` 的别名，但这属于实现细节，不要依赖它永远成立。

第二个参数是充当线程入口点的函数，最后一个参数是一个 `void *`，可以用来向线程传递自定义参数。现在函数 `hare` 将在一个单独的线程中运行，由操作系统抢占式地调度：

`thread-create/main.c`

```c
    int tracklen = *((int *) arg);
    while (tracklen) {
        tracklen--;
        uv_sleep(1000);
        fprintf(stderr, "Hare ran another step\n");
    }
    fprintf(stderr, "Hare done running!\n");
}
```

`pthread_join()` 允许目标线程通过第二个参数向调用线程传回一个值，而 `uv_thread_join()` 不支持这一点。要传递值，请使用[线程间通信](#线程间通信)。

## 同步原语

这一节刻意写得很简略。本书的主题并不是线程，所以我只把 libuv API 中出人意料之处罗列于此，其余内容请查阅 `pthreads(7)` 手册页。

### 互斥锁

互斥锁函数到 pthread 对应函数是**一一**对应的直接映射。

`libuv mutex functions`

```c
int uv_mutex_init(uv_mutex_t* handle);
int uv_mutex_init_recursive(uv_mutex_t* handle);
void uv_mutex_destroy(uv_mutex_t* handle);
void uv_mutex_lock(uv_mutex_t* handle);
int uv_mutex_trylock(uv_mutex_t* handle);
void uv_mutex_unlock(uv_mutex_t* handle);
```

`uv_mutex_init()`、`uv_mutex_init_recursive()` 和 `uv_mutex_trylock()` 成功时返回 0，否则返回错误码。

如果 libuv 编译时启用了调试，`uv_mutex_destroy()`、`uv_mutex_lock()` 和 `uv_mutex_unlock()` 出错时会调用 `abort()`。类似地，当错误*不是* `EAGAIN` 或 `EBUSY` 时，`uv_mutex_trylock()` 也会 abort。

libuv 支持递归互斥锁，但你不应依赖它，而且递归互斥锁不应与 `uv_cond_t` 变量一起使用。

默认的 BSD 互斥锁实现中，如果一个已持有互斥锁的线程再次尝试加锁，就会报错。例如，下面这样的结构：

```c
uv_mutex_init(a_mutex);
uv_mutex_lock(a_mutex);
uv_thread_create(thread_id, entry, (void *)a_mutex);
uv_mutex_lock(a_mutex);
// more things here
```

可以用来等待另一个线程完成某些初始化后再解锁 `a_mutex`；但如果程序运行在调试模式下，这会导致程序崩溃，或者在第二次调用 `uv_mutex_lock()` 时返回错误。

> **注意**：Windows 上的互斥锁总是递归的。

### 读写锁

读写锁是一种更细粒度的访问控制机制：两个读线程可以同时访问共享内存；当锁被某个读者持有时，写者不能获取锁；当写者持有锁时，读者和写者都不能获取锁。读写锁在数据库中被广泛使用。下面是一个玩具示例。

`locks/main.c - simple rwlocks`

```c
#include <stdio.h>
#include <uv.h>

uv_barrier_t blocker;
uv_rwlock_t numlock;
int shared_num;

void reader(void *n)
{
    int num = *(int *)n;
    int i;
    for (i = 0; i < 20; i++) {
        uv_rwlock_rdlock(&numlock);
        printf("Reader %d: acquired lock\n", num);
        printf("Reader %d: shared num = %d\n", num, shared_num);
        uv_rwlock_rdunlock(&numlock);
        printf("Reader %d: released lock\n", num);
    }
    uv_barrier_wait(&blocker);
}

void writer(void *n)
{
    int num = *(int *)n;
    int i;
    for (i = 0; i < 20; i++) {
        uv_rwlock_wrlock(&numlock);
        printf("Writer %d: acquired lock\n", num);
        shared_num++;
        printf("Writer %d: incremented shared num = %d\n", num, shared_num);
        uv_rwlock_wrunlock(&numlock);
        printf("Writer %d: released lock\n", num);
    }
    uv_barrier_wait(&blocker);
}

int main()
{
    uv_barrier_init(&blocker, 4);

    shared_num = 0;
    uv_rwlock_init(&numlock);

    uv_thread_t threads[3];

    int thread_nums[] = {1, 2, 1};
    uv_thread_create(&threads[0], reader, &thread_nums[0]);
    uv_thread_create(&threads[1], reader, &thread_nums[1]);

    uv_thread_create(&threads[2], writer, &thread_nums[2]);

    uv_barrier_wait(&blocker);
    uv_barrier_destroy(&blocker);

    uv_rwlock_destroy(&numlock);
    return 0;
}
```

运行它，你会观察到读者有时会重叠执行。在有多个写者的情况下，调度器通常会给写者更高的优先级，所以如果你加入两个写者，会看到两个写者往往都先完成，读者才重新有机会运行。

在上面的例子中我们还使用了屏障（barrier），让主线程能够等待所有读者和写者表明自己已经结束。

### 其他

libuv 还支持[信号量](https://en.wikipedia.org/wiki/Semaphore_(programming))、[条件变量](https://en.wikipedia.org/wiki/Monitor_(synchronization)#Condition_variables_2)和[屏障](https://en.wikipedia.org/wiki/Barrier_(computer_science))，它们的 API 与 pthread 中的对应物非常相似。

此外，libuv 提供了一个便利函数 `uv_once()`。多个线程可以带着同一个 guard（守卫对象）和一个函数指针去调用 `uv_once()`，**只有第一个会胜出，函数会被调用一次且仅一次**：

```c
/* Initialize guard */
static uv_once_t once_only = UV_ONCE_INIT;

int i = 0;

void increment() {
    i++;
}

void thread1() {
    /* ... work */
    uv_once(once_only, increment);
}

void thread2() {
    /* ... work */
    uv_once(once_only, increment);
}

int main() {
    /* ... spawn threads */
}
```

所有线程结束后，`i == 1`。

自 libuv v0.11.11 起，还新增了 `uv_key_t` 结构和相应的 [API](https://docs.libuv.org/en/v1.x/threading.html#thread-local-storage)，用于线程局部存储（thread-local storage）。

## libuv 工作队列

`uv_queue_work()` 是一个便利函数，它允许应用程序在单独的线程中运行一个任务，并在任务完成时触发一个回调。这个函数看似简单，它的诱人之处在于：它让几乎所有第三方库都有可能与事件循环范式配合使用。使用事件循环时，*务必确保在循环线程中周期性运行的函数不会在执行 I/O 时阻塞，也不是严重的 CPU 消耗大户*，否则事件循环的速度就会被拖慢，事件无法被全速处理。

然而，现存的很多代码都包含阻塞式函数（例如底层执行 I/O 的例程），想要保持响应性就得配合线程使用（经典的"每个客户端一个线程"服务器模型），而要让它们与事件循环库协同工作，通常需要自己动手搭一套在独立线程中运行任务的系统。libuv 正是为这一点提供了一个方便的抽象。

下面是一个受 [node.js is cancer](http://widgetsandshit.com/teddziuba/2011/10/node-js-is-cancer.html) 启发的简单例子。我们要计算斐波那契数，中途还会睡上片刻，但把它放到单独的线程里运行，这样这个阻塞且吃 CPU 的任务就不会妨碍事件循环做其他事情。

`queue-work/main.c - lazy fibonacci`

```c
void fib(uv_work_t *req) {
    int n = *(int *) req->data;
    if (random() % 2)
        sleep(1);
    else
        sleep(3);
    long fib = fib_(n);
    fprintf(stderr, "%dth fibonacci is %lu\n", n, fib);
}

void after_fib(uv_work_t *req, int status) {
    fprintf(stderr, "Done calculating %dth fibonacci\n", *(int *) req->data);
}
```

实际的任务函数很简单，看不出它将要在单独的线程中运行。线索在于 `uv_work_t` 结构：你可以通过它的 `void* data` 字段传递任意数据，并用它与线程双向通信。但请注意，如果两个线程可能同时在运行并且都在修改数据，一定要使用恰当的锁。

触发点是 `uv_queue_work`：

`queue-work/main.c`

```c
int main() {
    loop = uv_default_loop();

    int data[FIB_UNTIL];
    uv_work_t req[FIB_UNTIL];
    int i;
    for (i = 0; i < FIB_UNTIL; i++) {
        data[i] = i;
        req[i].data = (void *) &data[i];
        uv_queue_work(loop, &req[i], fib, after_fib);
    }

    return uv_run(loop, UV_RUN_DEFAULT);
}
```

线程函数会在单独的线程中被启动，并传入 `uv_work_t` 结构；一旦函数返回，*after* 函数就会在事件循环所在线程中被调用，传入的也是同一个结构。

为阻塞式库编写封装时，一种常见的模式是使用 baton（接力棒）来交换数据（参见"通用模式"一章中的 baton 一节）。

自 libuv `0.9.4` 版本起，多了一个 `uv_cancel()` 函数，它允许你取消 libuv 工作队列中的任务。只有*尚未开始*的任务才能被取消；如果任务*已经开始执行，或者已经执行完毕*，`uv_cancel()` **会失败**。

当用户请求终止程序时，`uv_cancel()` 很适合用来清理尚待执行的任务。例如，一个音乐播放器可能把多个目录排入队列以便扫描音频文件。如果用户要终止程序，程序应当尽快退出，而不是等所有待处理的请求都执行完。

让我们修改斐波那契的例子来演示 `uv_cancel()`。我们先为终止信号设置一个信号处理器。

`queue-cancel/main.c`

```c
int main() {
    loop = uv_default_loop();

    int data[FIB_UNTIL];
    int i;
    for (i = 0; i < FIB_UNTIL; i++) {
        data[i] = i;
        fib_reqs[i].data = (void *) &data[i];
        uv_queue_work(loop, &fib_reqs[i], fib, after_fib);
    }

    uv_signal_t sig;
    uv_signal_init(loop, &sig);
    uv_signal_start(&sig, signal_handler, SIGINT);

    return uv_run(loop, UV_RUN_DEFAULT);
}
```

当用户按下 `Ctrl+C` 触发信号时，我们对所有工作线程调用 `uv_cancel()`。对于尚在排队等待执行、可以取消的任务，`uv_cancel()` 会返回 `0`。

`queue-cancel/main.c`

```c
void signal_handler(uv_signal_t *req, int signum)
{
    printf("Signal received!\n");
    int i;
    for (i = 0; i < FIB_UNTIL; i++) {
        uv_cancel((uv_req_t*) &fib_reqs[i]);
    }
    uv_signal_stop(req);
}
```

对于被成功取消的任务，*after* 函数被调用时 `status` 被设置为 `UV_ECANCELED`。

`queue-cancel/main.c`

```c
void after_fib(uv_work_t *req, int status) {
    if (status == UV_ECANCELED)
        fprintf(stderr, "Calculation of %d cancelled.\n", *(int *) req->data);
}
```

`uv_cancel()` 也可以用于 `uv_fs_t` 和 `uv_getaddrinfo_t` 请求。对文件系统这一族函数来说，`uv_fs_t.errorno` 会被设置为 `UV_ECANCELED`。

> **提示**：一个设计良好的程序，应当有办法终止那些已经开始执行的长时运行的工作线程。这样的工作线程可以周期性地检查某个只有主进程才会设置的变量，以感知终止信号。

## 线程间通信

有时候，你会希望各个线程在运行*的同时*真的互相发送消息。比如，你可能在单独的线程里运行一个耗时的任务（也许就是通过 `uv_queue_work`），但想向主线程汇报进度。下面这个简单的例子演示了一个下载管理器如何向用户告知正在进行的下载的状态。

`progress/main.c`

```c
uv_loop_t *loop;
}

void print_progress(uv_async_t *handle) {
    double pct = atomic_load_explicit(&percentage, memory_order_acquire);
    fprintf(stderr, "Downloaded %.2f%%\n", pct);
}

int main() {
    loop = uv_default_loop();

    uv_work_t req;
    int size = 10240;
    req.data = (void*) &size;

    uv_async_init(loop, &async, print_progress);
    uv_queue_work(loop, &req, fake_download, after);
    return uv_run(loop, UV_RUN_DEFAULT);
}
```

这种异步线程通信是*作用在事件循环上的*：任何线程都可以是消息的发送方，但只有运行着 libuv 事件循环的线程才能成为接收方（更准确地说，接收方是事件循环本身）。libuv 每当收到消息时，就会带着 async 监视器调用回调（`print_progress`）。

> **警告**：必须认识到，由于消息的发送是*异步的*，回调可能在 `uv_async_send` 被另一个线程调用之后立即被触发，也可能过一段时间才被触发。libuv 还可能把多次 `uv_async_send` 合并起来，只调用一次你的回调。libuv 唯一保证的是——在调用 `uv_async_send` 之后，回调函数*至少被调用一次*。如果没有待处理的 `uv_async_send` 调用，回调不会被调用；如果你调用了两次或更多次，而 libuv 还没来得及运行回调，那么对于这多次 `uv_async_send` 调用，它*可能*只调用你的回调*一次*。但你的回调绝不会因为单个事件而被调用两次。

`progress/main.c`

```c
_Atomic double percentage;

void fake_download(uv_work_t *req) {
    int size = *((int*) req->data);
    int downloaded = 0;
    double pct;
    while (downloaded < size) {
        pct = downloaded * 100.0 / size;
        atomic_store_explicit(&percentage, pct, memory_order_release);
        uv_async_send(&async);

        sleep(1);
        downloaded += (200+random())%1000; // can only download max 1000bytes/sec,
                                           // but at least a 200;
    }
```

在下载函数中，我们更新进度指示器，并用 `uv_async_send` 把消息排入待发送队列。记住：`uv_async_send` 同样是非阻塞的，会立即返回。

`progress/main.c`

```c
void after(uv_work_t *req, int status) {
    fprintf(stderr, "Download complete\n");
    uv_close((uv_handle_t*) &async, NULL);
}
```

回调就是标准的 libuv 模式——从监视器中取出数据。

最后，记得要清理监视器，这一点很重要。

`progress/main.c`

```c
    // Ensure final 100% progress update is sent
    pct = 100.0;
    atomic_store_explicit(&percentage, pct, memory_order_release);
    uv_async_send(&async);
```

在上面这个滥用 `data` 字段的例子之后，[bnoordhuis](https://github.com/bnoordhuis) 指出：使用 `data` 字段不是线程安全的，`uv_async_send()` 实际上只是用来唤醒事件循环的。请使用互斥锁或读写锁来保证访问以正确的顺序进行。

> **注意**：互斥锁和读写锁**不能**在信号处理器内部使用，而 `uv_async_send` 可以。

必须使用 `uv_async_send` 的一个场景，是与那些要求线程亲和性（thread affinity）的库互操作时。例如在 node.js 中，一个 v8 引擎实例、它的上下文及其对象都绑定在启动 v8 实例的那个线程上；从另一个线程去操作 v8 的数据结构会导致未定义的结果。现在考虑某个绑定了第三方库的 node.js 模块，它大致是这样工作的：

1. 在 node 中，先为第三方库设置一个 JavaScript 回调，供它在需要更多信息时调用：

   ```js
   var lib = require('lib');
   lib.on_progress(function() {
       console.log("Progress");
   });

   lib.do();

   // do other stuff
   ```

2. `lib.do` 本应是非阻塞的，但第三方库是阻塞的，于是绑定层使用 `uv_queue_work`。

3. 在单独线程中执行的实际工作想要触发进度回调，却不能直接调进 v8 去操作 JavaScript，于是它使用 `uv_async_send`。

4. async 回调在主循环线程（也就是 v8 线程）中被调用，随后它与 v8 交互，触发那个 JavaScript 回调。
