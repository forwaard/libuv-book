# 网络

> 本章译自 libuv 官方文档 guide/networking.rst（libuv 1.52）

libuv 中的网络编程与直接使用 BSD 套接字接口没有太大区别：有些事情变得更简单了，一切都是非阻塞的，但概念保持不变。此外，libuv 还提供了一些实用函数，用来抽象那些繁琐、重复且底层的工作，比如用 BSD 套接字结构设置套接字、DNS 查询，以及调整各种套接字参数。

`uv_tcp_t` 和 `uv_udp_t` 结构体用于网络 I/O。

> **注意**：本章的代码示例只是为了演示某些 libuv API，并不是高质量代码的范例。它们会泄漏内存，也不总是正确地关闭连接。

## TCP

TCP 是面向连接的流协议，因此建立在 libuv 的流基础设施之上。

### 服务器

服务器套接字的流程如下：

1. 用 `uv_tcp_init` 初始化 TCP 句柄。
2. 用 `uv_tcp_bind` 绑定它。
3. 对该句柄调用 `uv_listen`，每当有客户端建立新连接时就会触发一个回调。
4. 用 `uv_accept` 接受连接。
5. 使用[流操作](#缓冲区与流)与客户端通信。

下面是一个简单的回显（echo）服务器：

**tcp-echo-server/main.c - 监听套接字**

```c
int main() {
    loop = uv_default_loop();

    uv_tcp_t server;
    uv_tcp_init(loop, &server);

    uv_ip4_addr("0.0.0.0", DEFAULT_PORT, &addr);

    uv_tcp_bind(&server, (const struct sockaddr*)&addr, 0);
    int r = uv_listen((uv_stream_t*) &server, DEFAULT_BACKLOG, on_new_connection);
    if (r) {
        fprintf(stderr, "Listen error %s\n", uv_strerror(r));
        return 1;
    }
    return uv_run(loop, UV_RUN_DEFAULT);
}
```

这里可以看到实用函数 `uv_ip4_addr` 的用法：它把人类可读的 IP 地址和端口对转换为 BSD 套接字 API 所需的 sockaddr_in 结构。反向转换可以用 `uv_ip4_name` 完成。

> **注意**：ip4 系列函数都有对应的 `uv_ip6_*` 版本。

大多数初始化设置函数都是同步的，因为它们是 CPU 密集型的。`uv_listen` 是我们回到 libuv 回调风格的地方。它的第二个参数是 backlog 队列——排队连接的最大长度。

当客户端发起连接时，回调需要为客户端套接字创建一个句柄，并用 `uv_accept` 把句柄关联起来。在这个例子中，我们还注册了对该流读取的兴趣。

**tcp-echo-server/main.c - 接受客户端**

```c
void on_new_connection(uv_stream_t *server, int status) {
    if (status < 0) {
        fprintf(stderr, "New connection error %s\n", uv_strerror(status));
        // error!
        return;
    }

    uv_tcp_t *client = (uv_tcp_t*) malloc(sizeof(uv_tcp_t));
    uv_tcp_init(loop, client);
    if (uv_accept(server, (uv_stream_t*) client) == 0) {
        uv_read_start((uv_stream_t*) client, alloc_buffer, echo_read);
    }
    else {
        uv_close((uv_handle_t*) client, on_close);
    }
}
```

其余的函数与流示例非常相似，可以直接在代码中查看。只需记住，当套接字不再需要时调用 `uv_close`。即使你对连接不感兴趣、不想接受它，也可以直接在 `uv_listen` 的回调中完成这一操作。

### 客户端

服务器端需要 bind/listen/accept，而客户端只需调用 `uv_tcp_connect` 即可。`uv_tcp_connect` 使用与 `uv_listen` 相同的 `uv_connect_cb` 风格回调。试试：

```c
uv_tcp_t* socket = (uv_tcp_t*)malloc(sizeof(uv_tcp_t));
uv_tcp_init(loop, socket);

uv_connect_t* connect = (uv_connect_t*)malloc(sizeof(uv_connect_t));

struct sockaddr_in dest;
uv_ip4_addr("127.0.0.1", 80, &dest);

uv_tcp_connect(connect, socket, (const struct sockaddr*)&dest, on_connect);
```

其中 `on_connect` 会在连接建立后被调用。回调接收 `uv_connect_t` 结构体，其成员 `.handle` 指向该套接字。

## UDP

[用户数据报协议（User Datagram Protocol）](https://en.wikipedia.org/wiki/User_Datagram_Protocol)提供无连接、不可靠的网络通信。因此 libuv 不为它提供流。libuv 通过 `uv_udp_t` 句柄（用于接收）和 `uv_udp_send_t` 请求（用于发送）以及相关函数提供非阻塞的 UDP 支持。不过，实际的读写 API 与普通的流读取非常相似。为了展示 UDP 的用法，示例展示了从 [DHCP](https://datatracker.ietf.org/doc/html/rfc2131) 服务器获取 IP 地址的第一阶段——DHCP Discover。

> **注意**：你必须以 **root** 身份运行 `udp-dhcp`，因为它使用了 1024 以下的知名端口号。

**udp-dhcp/main.c - 初始化并发送 UDP 数据包**

```c
int main() {
    loop = uv_default_loop();

    uv_udp_init(loop, &recv_socket);
    struct sockaddr_in recv_addr;
    uv_ip4_addr("0.0.0.0", 68, &recv_addr);
    uv_udp_bind(&recv_socket, (const struct sockaddr *)&recv_addr, UV_UDP_REUSEADDR);
    uv_udp_recv_start(&recv_socket, alloc_buffer, on_read);

    uv_udp_init(loop, &send_socket);
    struct sockaddr_in broadcast_addr;
    uv_ip4_addr("0.0.0.0", 0, &broadcast_addr);
    uv_udp_bind(&send_socket, (const struct sockaddr *)&broadcast_addr, 0);
    uv_udp_set_broadcast(&send_socket, 1);

    uv_udp_send_t send_req;
    uv_buf_t discover_msg = make_discover_msg();

    struct sockaddr_in send_addr;
    uv_ip4_addr("255.255.255.255", 67, &send_addr);
    uv_udp_send(&send_req, &send_socket, &discover_msg, 1, (const struct sockaddr *)&send_addr, on_send);

    return uv_run(loop, UV_RUN_DEFAULT);
}
```

> **注意**：IP 地址 `0.0.0.0` 用于绑定所有网络接口。IP 地址 `255.255.255.255` 是广播地址，意味着数据包会被发送到子网上的所有接口。端口 `0` 表示由操作系统随机分配端口。

首先，我们设置接收套接字，将其绑定到所有接口的 68 端口（DHCP 客户端端口），并在其上启动读取。这样就能读取到任何应答的 DHCP 服务器的响应。我们使用 UV_UDP_REUSEADDR 标志，以便与这台计算机上运行在同一端口的其他系统 DHCP 客户端和平共处。然后我们设置一个类似的发送套接字，并使用 `uv_udp_send` 向 67 端口（DHCP 服务器端口）发送一条*广播消息*。

**必须**设置广播标志，否则你会得到 `EACCES` 错误[^1]。所发送的具体消息内容与本书无关，有兴趣可以自行研究代码。和往常一样，如果出了问题，读写回调会收到一个 < 0 的状态码。

由于 UDP 套接字不与特定的对端建立连接，读回调会多收到一个参数，用以说明数据包的发送方。

如果没有更多数据可读，`nread` 可能为零。若 `addr` 为 NULL，表示没有数据可读（回调不应做任何事情）；若不为 NULL，则表示收到了来自 `addr` 主机的一个空数据报。如果分配器提供的缓冲区不足以容纳数据，`flags` 参数可能是 `UV_UDP_PARTIAL`。*在这种情况下，操作系统会丢弃装不下的数据*（这就是 UDP！）

**udp-dhcp/main.c - 读取数据包**

```c
void on_read(uv_udp_t *req, ssize_t nread, const uv_buf_t *buf, const struct sockaddr *addr, unsigned flags) {
    if (nread < 0) {
        fprintf(stderr, "Read error %s\n", uv_err_name(nread));
        uv_close((uv_handle_t*) req, NULL);
        free(buf->base);
        return;
    }

    char sender[17] = { 0 };
    uv_ip4_name((const struct sockaddr_in*) addr, sender, 16);
    fprintf(stderr, "Recv from %s\n", sender);

    // ... DHCP specific code
    unsigned int *as_integer = (unsigned int*)buf->base;
    unsigned int ipbin = ntohl(as_integer[4]);
    unsigned char ip[4] = {0};
    int i;
    for (i = 0; i < 4; i++)
        ip[i] = (ipbin >> i*8) & 0xff;
    fprintf(stderr, "Offered IP %d.%d.%d.%d\n", ip[3], ip[2], ip[1], ip[0]);

    free(buf->base);
    uv_udp_recv_stop(req);
}
```

### UDP 选项

#### 生存时间（TTL）

套接字发出数据包的 TTL 可以用 `uv_udp_set_ttl` 修改。

#### 仅 IPv6 协议栈

IPv6 套接字可以同时用于 IPv4 和 IPv6 通信。如果想把套接字限制为仅使用 IPv6，请把 `UV_UDP_IPV6ONLY` 标志传给 `uv_udp_bind`。

#### 组播

套接字可以使用以下函数加入/退出组播组：

```c
int uv_udp_set_membership(uv_udp_t* handle, const char* multicast_addr, const char* interface_addr, uv_membership membership);
```

其中 `membership` 是 `UV_JOIN_GROUP` 或 `UV_LEAVE_GROUP`。

组播的概念在[这篇指南](https://tldp.org/HOWTO/Multicast-HOWTO-2.html)中讲解得很好。

组播数据包的本地回环默认是开启的[^4]，可以使用 `uv_udp_set_multicast_loop` 关闭它。

组播数据包的生存时间（TTL）可以用 `uv_udp_set_multicast_ttl` 修改。

## 查询 DNS

libuv 提供异步 DNS 解析。为此，它提供了自己的 `getaddrinfo` 替代实现[^5]。在回调中，你可以对获得的地址执行普通的套接字操作。让我们连接 Libera.chat，看一个 DNS 解析的例子。

**dns/main.c**

```c
int main() {
    loop = uv_default_loop();

    struct addrinfo hints;
    hints.ai_family = PF_INET;
    hints.ai_socktype = SOCK_STREAM;
    hints.ai_protocol = IPPROTO_TCP;
    hints.ai_flags = 0;

    uv_getaddrinfo_t resolver;
    fprintf(stderr, "irc.libera.chat is... ");
    int r = uv_getaddrinfo(loop, &resolver, on_resolved, "irc.libera.chat", "6667", &hints);

    if (r) {
        fprintf(stderr, "getaddrinfo call error %s\n", uv_err_name(r));
        return 1;
    }
    return uv_run(loop, UV_RUN_DEFAULT);
}
```

如果 `uv_getaddrinfo` 返回非零值，说明设置阶段出了问题，你的回调根本不会被调用。`uv_getaddrinfo` 返回之后，所有参数都可以立即释放。`hostname`、`servname` 和 `hints` 结构在 [getaddrinfo 手册页](https://man7.org/linux/man-pages/man3/getaddrinfo.3.html)中有说明。回调可以是 `NULL`，此时该函数将同步运行。

在解析回调中，你可以从 `struct addrinfo(s)` 链表中任选一个 IP。这里也演示了 `uv_tcp_connect` 的用法。在回调中必须调用 `uv_freeaddrinfo`。

**dns/main.c**

```c
void on_resolved(uv_getaddrinfo_t *resolver, int status, struct addrinfo *res) {
    if (status < 0) {
        fprintf(stderr, "getaddrinfo callback error %s\n", uv_err_name(status));
        return;
    }

    char addr[17] = {'\0'};
    uv_ip4_name((struct sockaddr_in*) res->ai_addr, addr, 16);
    fprintf(stderr, "%s\n", addr);

    uv_connect_t *connect_req = (uv_connect_t*) malloc(sizeof(uv_connect_t));
    uv_tcp_t *socket = (uv_tcp_t*) malloc(sizeof(uv_tcp_t));
    uv_tcp_init(loop, socket);

    uv_tcp_connect(connect_req, socket, (const struct sockaddr*) res->ai_addr, on_connect);

    uv_freeaddrinfo(res);
}
```

libuv 还提供反向操作 [`uv_getnameinfo`](https://docs.libuv.org/en/v1.x/dns.html#c.uv_getnameinfo)。

## 网络接口

通过 libuv 的 `uv_interface_addresses` 可以获取系统网络接口的信息。下面这个简单的程序只是打印出所有接口的详细信息，让你了解有哪些可用字段。这可用于让服务在启动时绑定到 IP 地址。

**interfaces/main.c**

```c
int main() {
    char buf[512];
    uv_interface_address_t *info;
    int count, i;

    uv_interface_addresses(&info, &count);
    i = count;

    printf("Number of interfaces: %d\n", count);
    while (i--) {
        uv_interface_address_t interface_a = info[i];

        printf("Name: %s\n", interface_a.name);
        printf("Internal? %s\n", interface_a.is_internal ? "Yes" : "No");

        if (interface_a.address.address4.sin_family == AF_INET) {
            uv_ip4_name(&interface_a.address.address4, buf, sizeof(buf));
            printf("IPv4 address: %s\n", buf);
        }
        else if (interface_a.address.address4.sin_family == AF_INET6) {
            uv_ip6_name(&interface_a.address.address6, buf, sizeof(buf));
            printf("IPv6 address: %s\n", buf);
        }

        printf("\n");
    }

    uv_free_interface_addresses(info, count);
    return 0;
}
```

`is_internal` 对回环（loopback）接口为 true。注意，如果一个物理接口有多个 IPv4/IPv6 地址，该名称会被报告多次，每个地址报告一次。

---

[^1]: https://beej.us/guide/bgnet/html/#broadcast-packetshello-world

[^4]: https://tldp.org/HOWTO/Multicast-HOWTO-6.html#ss6.1

[^5]: libuv 在 libuv 线程池中使用系统的 `getaddrinfo`。libuv v0.8.0 及更早版本还包含 [c-ares](https://c-ares.org/) 作为备选方案，但在 v0.9.0 中已移除。
