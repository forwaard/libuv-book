> 本章译自 libuv 官方文档 guide/processes.rst（libuv 1.52）

# 进程

libuv 提供了相当完善的子进程管理能力，抽象掉了平台之间的差异，并允许通过流（stream）或命名管道与子进程通信。

在 Unix 中有一个惯例：让每个进程只做一件事，并把它做好。在这种思路下，一个进程常常借助多个子进程来完成任务（类似于在 shell 中使用管道）。与使用线程和共享内存的模型相比，基于消息传递的多进程模型通常也更容易推理和理解。

针对事件驱动程序，一种常见的质疑是它们无法利用现代计算机的多核。在多线程程序中，内核可以进行调度，把不同的线程分配到不同的核心上，从而提升性能；但事件循环只有一个线程。解决办法是改为启动多个进程，每个进程运行一个事件循环，并被分配到单独的 CPU 核心上。

## 创建子进程

最简单的情形是你只想启动一个进程，并知道它何时退出。这可以通过 `uv_spawn` 来实现。

`spawn/main.c`

```c
uv_loop_t *loop;
uv_process_t child_req;
uv_process_options_t options;

int main() {
    loop = uv_default_loop();

    char* args[3];
    args[0] = "mkdir";
    args[1] = "test-dir";
    args[2] = NULL;

    options.exit_cb = on_exit;
    options.file = "mkdir";
    options.args = args;

    int r;
    if ((r = uv_spawn(loop, &child_req, &options))) {
        fprintf(stderr, "%s\n", uv_strerror(r));
        return 1;
    } else {
        fprintf(stderr, "Launched process with ID %d\n", child_req.pid);
    }

    return uv_run(loop, UV_RUN_DEFAULT);
}
```

> **注意**：由于 `options` 是全局变量，它会被隐式地初始化为零值。如果你把 `options` 改成局部变量，请记得将它初始化，把所有未使用的字段都清空：
>
> ```c
> uv_process_options_t options = {0};
> ```

`uv_process_t` 结构体只充当句柄，所有选项都通过 `uv_process_options_t` 来设置。若只是想简单地启动一个进程，你只需要设置 `file` 和 `args` 字段。`file` 是要执行的程序。由于 `uv_spawn` 内部使用 `execvp(3)`，因此无需提供完整路径。最后，按照底层约定，**参数数组必须比参数个数多一，且最后一个元素为 NULL**。

调用 `uv_spawn` 之后，`uv_process_t.pid` 中会保存子进程的进程 ID。

退出回调会带着*退出状态*（exit status）以及导致退出的*信号*类型被调用。

注意，**不要**在退出回调被调用之前调用 `uv_close`，这一点很重要。

`spawn/main.c`

```c
void on_exit(uv_process_t *req, int64_t exit_status, int term_signal) {
    fprintf(stderr, "Process exited with status %" PRId64 ", signal %d\n", exit_status, term_signal);
    uv_close((uv_handle_t*) req, NULL);
}
```

进程退出之后，**必须**关闭这个进程监视器。

## 修改进程参数

在子进程启动之前，你可以利用 `uv_process_options_t` 中的字段来控制它的执行环境。

### 修改执行目录

把 `uv_process_options_t.cwd` 设置为相应的目录即可。

### 设置环境变量

`uv_process_options_t.env` 是一个以 NULL 结尾的字符串数组，其中每个字符串形如 `VAR=VALUE`，用于为进程设置环境变量。将其设为 `NULL` 则继承父（当前）进程的环境变量。

### 选项标志

把 `uv_process_options_t.flags` 设置为下列标志的按位或，可以改变子进程的行为：

- `UV_PROCESS_SETUID` — 将子进程的执行用户 ID 设置为 `uv_process_options_t.uid`。
- `UV_PROCESS_SETGID` — 将子进程的执行组 ID 设置为 `uv_process_options_t.gid`。

修改 UID/GID 只在 Unix 上受支持，在 Windows 上 `uv_spawn` 会失败并返回 `UV_ENOTSUP`。

- `UV_PROCESS_WINDOWS_VERBATIM_ARGUMENTS` — 在 Windows 上不对 `uv_process_options_t.args` 做任何引号包裹或转义处理。在 Unix 上被忽略。
- `UV_PROCESS_DETACHED` — 在新的会话（session）中启动子进程，这样即使父进程退出，子进程也会继续运行。参见下文示例。

## 分离进程

传入 `UV_PROCESS_DETACHED` 标志可以用来启动守护进程，或者启动独立于父进程的子进程，使父进程的退出不影响它们。

`detach/main.c`

```c
int main() {
    loop = uv_default_loop();

    char* args[3];
    args[0] = "sleep";
    args[1] = "100";
    args[2] = NULL;

    options.exit_cb = NULL;
    options.file = "sleep";
    options.args = args;
    options.flags = UV_PROCESS_DETACHED;

    int r;
    if ((r = uv_spawn(loop, &child_req, &options))) {
        fprintf(stderr, "%s\n", uv_strerror(r));
        return 1;
    }
    fprintf(stderr, "Launched sleep with PID %d\n", child_req.pid);
    uv_unref((uv_handle_t*) &child_req);

    return uv_run(loop, UV_RUN_DEFAULT);
}
```

请记住，句柄仍然在监视着子进程，所以你的程序不会退出。如果你想要更*发射后不管*（fire-and-forget）的效果，可以使用 `uv_unref()`。

## 向进程发送信号

libuv 封装了 Unix 上的标准 `kill(2)` 系统调用，并在 Windows 上实现了语义类似的版本，但有一个**注意点**：`SIGTERM`、`SIGINT` 和 `SIGKILL` 都会导致进程被终止。`uv_kill` 的签名为：

```c
uv_err_t uv_kill(int pid, int signum);
```

对于用 libuv 启动的进程，你可以改用 `uv_process_kill`，它接受 `uv_process_t` 监视器（而不是 pid）作为第一个参数。在这种情况下，**记得要**在退出回调被调用之*后*对监视器调用 `uv_close`。

## 信号

libuv 对 Unix 信号做了封装，同时也提供[部分 Windows 支持](https://docs.libuv.org/en/v1.x/signal.html#signal)。

使用 `uv_signal_init()` 初始化一个句柄，并将它与一个事件循环关联起来。要在该句柄上监听特定的信号，可使用 `uv_signal_start()` 并传入处理函数。每个处理函数只能关联一个信号编号，后续对 `uv_signal_start()` 的调用会覆盖之前的关联。使用 `uv_signal_stop()` 停止监听。下面是一个演示各种用法的小例子：

`signal/main.c`

```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <uv.h>

uv_loop_t* create_loop()
{
    uv_loop_t *loop = malloc(sizeof(uv_loop_t));
    if (loop) {
      uv_loop_init(loop);
    }
    return loop;
}

void signal_handler(uv_signal_t *handle, int signum)
{
    printf("Signal received: %d\n", signum);
    uv_signal_stop(handle);
}

// two signal handlers in one loop
void thread1_worker(void *userp)
{
    uv_loop_t *loop1 = create_loop();

    uv_signal_t sig1a, sig1b;
    uv_signal_init(loop1, &sig1a);
    uv_signal_start(&sig1a, signal_handler, SIGUSR1);

    uv_signal_init(loop1, &sig1b);
    uv_signal_start(&sig1b, signal_handler, SIGUSR1);

    uv_run(loop1, UV_RUN_DEFAULT);
}

// two signal handlers, each in its own loop
void thread2_worker(void *userp)
{
    uv_loop_t *loop2 = create_loop();
    uv_loop_t *loop3 = create_loop();

    uv_signal_t sig2;
    uv_signal_init(loop2, &sig2);
    uv_signal_start(&sig2, signal_handler, SIGUSR1);

    uv_signal_t sig3;
    uv_signal_init(loop3, &sig3);
    uv_signal_start(&sig3, signal_handler, SIGUSR1);

    while (uv_run(loop2, UV_RUN_NOWAIT) || uv_run(loop3, UV_RUN_NOWAIT)) {
    }
}

int main()
{
    printf("PID %d\n", getpid());

    uv_thread_t thread1, thread2;

    uv_thread_create(&thread1, thread1_worker, 0);
    uv_thread_create(&thread2, thread2_worker, 0);

    uv_thread_join(&thread1);
    uv_thread_join(&thread2);
    return 0;
}
```

> **注意**：`uv_run(loop, UV_RUN_NOWAIT)` 与 `uv_run(loop, UV_RUN_ONCE)` 的相似之处在于二者都只处理一个事件。区别在于：没有待处理事件时，`UV_RUN_ONCE` 会阻塞，而 `UV_RUN_NOWAIT` 会立即返回。我们使用 `NOWAIT`，是为了避免其中一个循环因为另一个循环没有待处理的活动而得不到运行机会（被"饿死"）。

给该进程发送 `SIGUSR1`，你会发现处理函数被调用了 4 次——每个 `uv_signal_t` 各一次。处理函数只是把每个句柄停掉，程序随之退出。这种向所有处理函数广播式的分发非常有用：一个使用多个事件循环的服务器只需让每个循环都为 `SIGINT` 添加一个监视器，就能确保在终止之前所有数据都被安全地保存。

## 子进程 I/O

一个普通的新建进程拥有自己的一组文件描述符，其中 0、1、2 分别是 `stdin`、`stdout` 和 `stderr`。有时你可能想与子进程共享文件描述符。举例来说，也许你的应用会启动一个子命令，你希望错误信息写入日志文件，同时忽略 `stdout`。这时你希望子进程的 `stderr` 与父进程的 stderr 相同。为此，libuv 支持*继承*文件描述符。在本例中，我们调用这个测试程序：

`proc-streams/test.c`

```c
#include <stdio.h>

int main()
{
    fprintf(stderr, "This is stderr\n");
    printf("This is stdout\n");
    return 0;
}
```

实际运行的程序 `proc-streams` 在执行它的同时只共享 `stderr`。子进程的文件描述符通过 `uv_process_options_t` 中的 `stdio` 字段来设置。首先把 `stdio_count` 字段设置为要设置的文件描述符的数量。`uv_process_options_t.stdio` 是一个 `uv_stdio_container_t` 数组，该结构定义为：

```c
typedef struct uv_stdio_container_s {
    uv_stdio_flags flags;

    union {
        uv_stream_t* stream;
        int fd;
    } data;
} uv_stdio_container_t;
```

其中 `flags` 可以取若干个值。如果某个描述符不会被用到，就使用 `UV_IGNORE`。如果前三个 `stdio` 项都被标记为 `UV_IGNORE`，它们会被重定向到 `/dev/null`。

由于我们想传入一个已有的描述符，所以使用 `UV_INHERIT_FD`，然后把 `fd` 设置为 `stderr`。

`proc-streams/main.c`

```c
int main() {
    loop = uv_default_loop();

    /* ... */

    options.stdio_count = 3;
    uv_stdio_container_t child_stdio[3];
    child_stdio[0].flags = UV_IGNORE;
    child_stdio[1].flags = UV_IGNORE;
    child_stdio[2].flags = UV_INHERIT_FD;
    child_stdio[2].data.fd = 2;
    options.stdio = child_stdio;

    options.exit_cb = on_exit;
    options.file = args[0];
    options.args = args;

    int r;
    if ((r = uv_spawn(loop, &child_req, &options))) {
        fprintf(stderr, "%s\n", uv_strerror(r));
        return 1;
    }

    return uv_run(loop, UV_RUN_DEFAULT);
}
```

运行 `proc-stream`，你会看到只有 "This is stderr" 这一行被显示出来。试着把 `stdout` 也标记为继承，再观察输出。

把这种重定向应用到流（stream）上也非常简单。将 `flags` 设为 `UV_INHERIT_STREAM`，并把 `data.stream` 设置为父进程中的某个流，子进程就可以把这个流当作标准 I/O 来使用。这可以用来实现类似 [CGI](https://en.wikipedia.org/wiki/Common_Gateway_Interface) 的功能。

CGI 脚本/可执行程序的一个示例如下：

`cgi/tick.c`

```c
#include <stdio.h>
#include <unistd.h>

int main() {
    int i;
    for (i = 0; i < 10; i++) {
        printf("tick\n");
        fflush(stdout);
        sleep(1);
    }
    printf("BOOM!\n");
    return 0;
}
```

这个 CGI 服务器把本章的概念与网络编程一章的概念结合起来：每个客户端会被发送十个 tick，之后连接被关闭。

`cgi/main.c`

```c
void on_new_connection(uv_stream_t *server, int status) {
    if (status == -1) {
        // error!
        return;
    }

    uv_tcp_t *client = (uv_tcp_t*) malloc(sizeof(uv_tcp_t));
    uv_tcp_init(loop, client);
    if (uv_accept(server, (uv_stream_t*) client) == 0) {
        invoke_cgi_script(client);
    }
    else {
        uv_close((uv_handle_t*) client, NULL);
    }
}
```

这里我们只是简单地接受 TCP 连接，并把套接字（*流*）传递给 `invoke_cgi_script`。

`cgi/main.c`

```c
    args[1] = NULL;

    /* ... finding the executable path and setting up arguments ... */

    options.stdio_count = 3;
    uv_stdio_container_t child_stdio[3];
    child_stdio[0].flags = UV_IGNORE;
    child_stdio[1].flags = UV_INHERIT_STREAM;
    child_stdio[1].data.stream = (uv_stream_t*) client;
    child_stdio[2].flags = UV_IGNORE;
    options.stdio = child_stdio;

    options.exit_cb = cleanup_handles;
    options.file = args[0];
    options.args = args;

    // Set this so we can close the socket after the child process exits.
    child_req.data = (void*) client;
    int r;
    if ((r = uv_spawn(loop, &child_req, &options))) {
        fprintf(stderr, "%s\n", uv_strerror(r));
```

CGI 脚本的 `stdout` 被设置为该套接字，这样 tick 脚本打印的任何内容都会被发送给客户端。借助进程，我们可以把读写的缓冲工作交给操作系统来完成，就便利性而言非常出色。只是要当心：创建进程是一笔开销不小的操作。

## 父子进程 IPC

通过把 `uv_stdio_container_t.flags` 设置为 `UV_CREATE_PIPE` 与 `UV_READABLE_PIPE` 或 `UV_WRITABLE_PIPE` 的按位组合，父子进程可以在一条管道上进行单向或双向通信。读/写标志是从子进程的视角来看的。在这种情况下，`uv_stream_t* stream` 字段必须指向一个已初始化、尚未打开的 `uv_pipe_t` 实例。

### 新建 stdio 管道

`uv_pipe_t` 结构所代表的不只是 [`pipe(7)`](https://man7.org/linux/man-pages/man7/pipe.7.html)（也就是 `|`），它支持任何流式的、类文件的对象。在 Windows 上，符合这一描述的对象只有[命名管道（Named Pipe）](https://learn.microsoft.com/en-us/windows/win32/ipc/named-pipes)。在 Unix 上，它可以是 [Unix 域套接字](https://man7.org/linux/man-pages/man7/unix.7.html)，可以派生自 [`mkfifo(1)`](https://man7.org/linux/man-pages/man1/mkfifo.1.html)，也可以真的就是一条 [`pipe(7)`](https://man7.org/linux/man-pages/man7/pipe.7.html) 管道。当 `uv_spawn` 因为 `UV_CREATE_PIPE` 标志而初始化一个 `uv_pipe_t` 时，它会选择创建一个 [`socketpair(2)`](https://man7.org/linux/man-pages/man2/socketpair.2.html)。

这样做的目的是让多个 libuv 进程能够通过 IPC 相互通信，下文会讨论这一点。

### 任意进程间的 IPC

由于域套接字（domain socket，在 Windows 上这一节的内容同样适用于命名管道）[^\*] 可以拥有一个众所周知的名字和文件系统中的位置，它们可以用于不相关的进程之间的 IPC。开源桌面环境所使用的 [D-Bus](https://www.freedesktop.org/wiki/Software/dbus) 系统就用域套接字来做事件通知：当联系人上线或检测到新硬件时，各种应用程序都可以做出响应。MySQL 服务器同样运行着一个域套接字，客户端可以通过它与服务器交互。

使用域套接字时，通常遵循客户端-服务器模式，套接字的创建者/拥有者充当服务器。在完成初始设置之后，消息传递与 TCP 没有任何区别，因此我们将复用回显服务器的例子。

`pipe-echo-server/main.c`

```c
void remove_sock(int sig) {
    uv_fs_t req;
    uv_fs_unlink(loop, &req, PIPENAME, NULL);
    exit(0);
}

int main() {
    loop = uv_default_loop();

    uv_pipe_t server;
    uv_pipe_init(loop, &server, 0);

    signal(SIGINT, remove_sock);

    int r;
    if ((r = uv_pipe_bind(&server, PIPENAME))) {
        fprintf(stderr, "Bind error %s\n", uv_err_name(r));
        return 1;
    }
    if ((r = uv_listen((uv_stream_t*) &server, 128, on_new_connection))) {
        fprintf(stderr, "Listen error %s\n", uv_err_name(r));
        return 2;
    }
    return uv_run(loop, UV_RUN_DEFAULT);
}
```

我们把套接字命名为 `echo.sock`，也就是说它会被创建在当前目录下。就流 API 而言，这个套接字的行为与 TCP 套接字并无二致。你可以用 [`socat`](http://www.dest-unreach.org/socat/) 来测试这个服务器：

```
$ socat - /path/to/socket
```

想要连接域套接字的客户端会使用：

```c
void uv_pipe_connect(uv_connect_t *req, uv_pipe_t *handle, const char *name, uv_connect_cb cb);
```

其中 `name` 就是 `echo.sock` 之类的名字。在 Unix 系统上，`name` 必须指向一个有效的文件（例如 `/tmp/echo.sock`）；在 Windows 上，`name` 遵循 `\\?\pipe\echo.sock` 这样的格式。

### 通过管道发送文件描述符

域套接字的一个妙处在于：文件描述符可以通过域套接字在进程之间传递。这使得进程可以把 I/O 移交给其他进程。应用场景包括负载均衡服务器、工作进程（worker process），以及其他充分利用 CPU 的方式。目前 libuv 只支持通过管道发送 **TCP 套接字或其他管道**。

为了演示，我们来看一个把客户端以轮询（round-robin）方式移交给工作进程的回显服务器实现。这个程序稍微复杂一些，虽然本书只收录了片段，但建议通读完整代码以真正理解它。

工作进程相当简单，因为文件描述符是由主进程（master）移交给它的。

`multi-echo-server/worker.c`

```c
uv_loop_t *loop;
uv_pipe_t queue;
int main() {
    loop = uv_default_loop();

    uv_pipe_init(loop, &queue, 1 /* ipc */);
    uv_pipe_open(&queue, 0);
    uv_read_start((uv_stream_t*)&queue, alloc_buffer, on_new_connection);
    return uv_run(loop, UV_RUN_DEFAULT);
}
```

`queue` 是连接到主进程的那条管道，新的文件描述符沿着它被发送过来。重要的是，要把 `uv_pipe_init` 的 `ipc` 参数设为 1，以表明这条管道将用于进程间通信！由于主进程会把文件句柄写入工作进程的标准输入，我们用 `uv_pipe_open` 把管道连接到 `stdin`。

`multi-echo-server/worker.c`

```c
void on_new_connection(uv_stream_t *q, ssize_t nread, const uv_buf_t *buf) {
    if (nread < 0) {
        if (nread != UV_EOF)
            fprintf(stderr, "Read error %s\n", uv_err_name(nread));
        uv_close((uv_handle_t*) q, NULL);
        return;
    }

    uv_pipe_t *pipe = (uv_pipe_t*) q;
    if (!uv_pipe_pending_count(pipe)) {
        fprintf(stderr, "No pending count\n");
        return;
    }

    uv_handle_type pending = uv_pipe_pending_type(pipe);
    assert(pending == UV_TCP);

    uv_tcp_t *client = (uv_tcp_t*) malloc(sizeof(uv_tcp_t));
    uv_tcp_init(loop, client);
    if (uv_accept(q, (uv_stream_t*) client) == 0) {
        uv_os_fd_t fd;
        uv_fileno((const uv_handle_t*) client, &fd);
        fprintf(stderr, "Worker %d: Accepted fd %d\n", getpid(), fd);
        uv_read_start((uv_stream_t*) client, alloc_buffer, echo_read);
    }
    else {
        uv_close((uv_handle_t*) client, NULL);
    }
}
```

我们先调用 `uv_pipe_pending_count()`，确保确实有句柄可以读出。如果你的程序可能处理多种类型的句柄，可以用 `uv_pipe_pending_type()` 来判断类型。虽然 `accept` 出现在这段代码里看起来有些奇怪，但实际上是说得通的：`accept` 传统上做的事情就是从另一个文件描述符（监听套接字）那里取得一个文件描述符（客户端），而这正是我们在这里所做的——从 `queue` 中取出文件描述符（`client`）。从这一刻起，工作进程做的就是标准的回显服务器那套事情。

接下来看主进程，了解一下工作进程是如何被启动以实现负载均衡的。

`multi-echo-server/main.c`

```c
struct child_worker {
    uv_process_t req;
    uv_process_options_t options;
    uv_pipe_t pipe;
} *workers;
```

`child_worker` 结构体把进程本身以及主进程与各个进程之间的管道包装在一起。

`multi-echo-server/main.c`

```c
void setup_workers() {
    round_robin_counter = 0;

    // ...

    // launch same number of workers as number of CPUs
    uv_cpu_info_t *info;
    int cpu_count;
    uv_cpu_info(&info, &cpu_count);
    uv_free_cpu_info(info, cpu_count);

    child_worker_count = cpu_count;

    workers = calloc(cpu_count, sizeof(struct child_worker));
    while (cpu_count--) {
        struct child_worker *worker = &workers[cpu_count];
        uv_pipe_init(loop, &worker->pipe, 1);

        uv_stdio_container_t child_stdio[3];
        child_stdio[0].flags = UV_CREATE_PIPE | UV_READABLE_PIPE;
        child_stdio[0].data.stream = (uv_stream_t*) &worker->pipe;
        child_stdio[1].flags = UV_IGNORE;
        child_stdio[2].flags = UV_INHERIT_FD;
        child_stdio[2].data.fd = 2;

        worker->options.stdio = child_stdio;
        worker->options.stdio_count = 3;

        worker->options.exit_cb = close_process_handle;
        worker->options.file = args[0];
        worker->options.args = args;

        uv_spawn(loop, &worker->req, &worker->options);
        fprintf(stderr, "Started worker %d\n", worker->req.pid);
    }
}
```

在设置工作进程时，我们用到了 libuv 一个很方便的函数 `uv_cpu_info` 来获取 CPU 数量，从而启动数量相等的工作进程。同样重要的是，要把充当 IPC 通道的管道的第三个参数初始化为 1。然后我们声明子进程的 `stdin` 将是一条可读管道（从子进程的视角看）。到这里为止一切都直截了当：工作进程被启动，等待文件描述符写入它们的标准输入。

真正接收客户端套接字并把它传递给轮询中下一个工作进程的地方是 `on_new_connection`（TCP 基础设施在 `main()` 中初始化）。

`multi-echo-server/main.c`

```c
void on_new_connection(uv_stream_t *server, int status) {
    if (status == -1) {
        // error!
        return;
    }

    uv_tcp_t *client = (uv_tcp_t*) malloc(sizeof(uv_tcp_t));
    uv_tcp_init(loop, client);
    if (uv_accept(server, (uv_stream_t*) client) == 0) {
        uv_write_t *write_req = (uv_write_t*) malloc(sizeof(uv_write_t));
        dummy_buf = uv_buf_init("a", 1);
        struct child_worker *worker = &workers[round_robin_counter];
        uv_write2(write_req, (uv_stream_t*) &worker->pipe, &dummy_buf, 1, (uv_stream_t*) client, NULL);
        round_robin_counter = (round_robin_counter + 1) % child_worker_count;
    }
    else {
        uv_close((uv_handle_t*) client, NULL);
    }
}
```

`uv_write2` 调用处理了所有的抽象细节，我们只需把句柄（`client`）作为相应参数传入即可。至此，我们的多进程回显服务器就可以运转了。

感谢 Kyle [指出](https://github.com/nikhilm/uvbook/issues/56)`uv_write2()` 在发送句柄时也需要一个非空的缓冲区。

---

[^\*]: 在这一节中，域套接字在 Windows 上同样代指命名管道。
