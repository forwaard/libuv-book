> 本章译自 libuv 官方文档 guide/basics.rst（libuv 1.52）

# libuv 基础

libuv 强制采用**异步**、**事件驱动**的编程风格。它的核心工作是提供一个事件循环（event loop），并基于回调（callback）对 I/O 及其他活动发出通知。libuv 还提供了一系列核心工具：定时器、非阻塞网络支持、异步文件系统访问、子进程等等。

## 事件循环

在事件驱动的编程模型中，应用程序对某些事件表达出兴趣，并在这些事件发生时作出响应。从操作系统收集事件、或监视其他事件来源的工作由 libuv 负责，用户只需注册回调，即可在事件发生时被调用。事件循环通常会**永远**运行下去。用伪代码表示：

```python
while there are still events to process:
    e = get the next event
    if there is a callback associated with e:
        call the callback
```

事件的例子有：

* 文件已可写入
* 某个套接字（socket）有数据可读
* 某个定时器已超时

这个事件循环被封装在 `uv_run()` 中——它是使用 libuv 时最核心的函数。

系统程序最常见的活动是处理输入和输出，而不是大量的数值计算。使用传统输入/输出函数（`read`、`fprintf` 等）的问题在于它们是**阻塞**的。真正写入硬盘或从网络读取数据所耗费的时间，与处理器的速度相比长得不成比例。这些函数要等到任务完成才返回，期间你的程序什么事都做不了。对于追求高性能的程序来说，这是一大障碍，因为其他活动和其他 I/O 操作都只能干等。

标准解决方案之一是使用线程（thread）。每个阻塞的 I/O 操作都在单独的线程（或线程池）中启动。当阻塞函数在某个线程中被调用时，操作系统可以调度另一个真正需要 CPU 的线程去运行。

libuv 采用的是另一种风格，即**异步、非阻塞**（asynchronous, non-blocking）风格。大多数现代操作系统都提供事件通知子系统。举例来说，对一个套接字发起普通的 `read` 调用会一直阻塞，直到发送方真的发来数据；替代做法是，应用程序请求操作系统替它监视这个套接字，并把事件通知放进队列。应用程序可以在自己方便的时候查看这些事件（也许在此之前先做一些数值计算，把处理器能力用满），然后取出数据。说它是**异步**的，是因为应用程序在一个时间点表达兴趣，而在另一个时间点（时间与空间上的另一点）使用数据；说它是**非阻塞**的，是因为应用程序进程可以自由地去做其他任务。这与 libuv 的事件循环方式契合得很好，因为操作系统事件可以被当作又一种 libuv 事件来处理。非阻塞保证了其他事件能在到达时被尽快继续处理[^1]。

> **注意**：
>
> I/O 在后台如何运行并不是我们需要关心的，但受限于计算机硬件的工作方式——线程是处理器的基本调度单元——libuv 和操作系统通常会运行后台/工作线程，和/或采用轮询（polling）的方式，以非阻塞的方式完成任务。

libuv 核心开发者之一 Bert Belder 有一个小视频，讲解了 libuv 的架构及其来龙去脉。如果你此前没有使用过 libuv，这个视频短小而值得一看。

libuv 的事件循环在[官方文档](https://docs.libuv.org/en/v1.x/design.html#the-i-o-loop)中有更详细的说明。

[视频链接（Bert Belder 讲 libuv 架构）](https://www.youtube-nocookie.com/embed/nGn60vDSxQ4)

## Hello World

基础铺垫完毕，现在来写第一个 libuv 程序。它什么也不做，只是启动一个会立即退出的循环。

**helloworld/main.c**

```c
#include <stdio.h>
#include <stdlib.h>
#include <uv.h>

int main() {
    uv_loop_t *loop = malloc(sizeof(uv_loop_t));
    uv_loop_init(loop);

    printf("Now quitting.\n");
    uv_run(loop, UV_RUN_DEFAULT);

    uv_loop_close(loop);
    free(loop);
    return 0;
}
```

这个程序会立即退出，因为它没有任何事件需要处理。必须通过各种 API 函数告诉 libuv 的事件循环要监视哪些事件。

从 libuv v1.0 开始，用户应当先用 `uv_loop_init(uv_loop_t *)` 初始化之前自行分配循环所需的内存。这样你就可以接入自定义的内存管理。记得用 `uv_loop_close(uv_loop_t *)` 反初始化循环，然后再释放存储空间。本书的示例从不关闭循环，因为程序在循环结束后就退出了，系统会回收内存。生产级项目，尤其是长期运行的系统程序，应当注意正确地释放资源。

### 默认循环

libuv 提供了一个默认循环，可以通过 `uv_default_loop()` 获取。如果你只需要一个循环，就应当使用它。

**default-loop/main.c**

```c
#include <stdio.h>
#include <uv.h>

int main() {
    uv_loop_t *loop = uv_default_loop();

    printf("Default loop.\n");
    uv_run(loop, UV_RUN_DEFAULT);

    uv_loop_close(loop);
    return 0;
}
```

> **注意**：
>
> node.js 使用默认循环作为它的主循环。如果你在编写绑定（binding），应当留意这一点。

<a id="libuv-error-handling"></a>

## 错误处理

可能失败的初始化函数或同步函数在出错时返回一个负数。可能失败的异步函数则通过回调传入一个状态参数。错误消息被定义为 `UV_E*` [常量](https://docs.libuv.org/en/v1.x/errors.html#error-constants)。

你可以使用 `uv_strerror(int)` 和 `uv_err_name(int)` 函数分别获得描述错误的 `const char *` 或错误名称。

I/O 读取回调（例如文件和套接字的回调）会收到一个参数 `nread`。如果 `nread` 小于 0，说明发生了错误（`UV_EOF` 表示读到文件末尾，你可能希望对它做区别处理）。

## 句柄与请求

libuv 的工作方式是让用户对特定事件表达兴趣。这通常通过创建一个指向 I/O 设备、定时器或进程的**句柄**（handle）来完成。句柄是不透明的结构体，命名为 `uv_TYPE_t`，其中 type 表示该句柄的用途。

**libuv 的各类 watcher**

```c
/* Handle types. */
typedef struct uv_loop_s uv_loop_t;
typedef struct uv_handle_s uv_handle_t;
typedef struct uv_dir_s uv_dir_t;
typedef struct uv_stream_s uv_stream_t;
typedef struct uv_tcp_s uv_tcp_t;
typedef struct uv_udp_s uv_udp_t;
typedef struct uv_pipe_s uv_pipe_t;
typedef struct uv_tty_s uv_tty_t;
typedef struct uv_poll_s uv_poll_t;
typedef struct uv_timer_s uv_timer_t;
typedef struct uv_prepare_s uv_prepare_t;
typedef struct uv_check_s uv_check_t;
typedef struct uv_idle_s uv_idle_t;
typedef struct uv_async_s uv_async_t;
typedef struct uv_process_s uv_process_t;
typedef struct uv_fs_event_s uv_fs_event_t;
typedef struct uv_fs_poll_s uv_fs_poll_t;
typedef struct uv_signal_s uv_signal_t;

/* Request types. */
typedef struct uv_req_s uv_req_t;
typedef struct uv_getaddrinfo_s uv_getaddrinfo_t;
typedef struct uv_getnameinfo_s uv_getnameinfo_t;
typedef struct uv_shutdown_s uv_shutdown_t;
typedef struct uv_write_s uv_write_t;
typedef struct uv_connect_s uv_connect_t;
typedef struct uv_udp_send_s uv_udp_send_t;
typedef struct uv_fs_s uv_fs_t;
typedef struct uv_work_s uv_work_t;
typedef struct uv_random_s uv_random_t;

/* None of the above. */
typedef struct uv_env_item_s uv_env_item_t;
typedef struct uv_cpu_info_s uv_cpu_info_t;
typedef struct uv_interface_address_s uv_interface_address_t;
typedef struct uv_dirent_s uv_dirent_t;
typedef struct uv_passwd_s uv_passwd_t;
typedef struct uv_utsname_s uv_utsname_t;
typedef struct uv_statfs_s uv_statfs_t;
```

句柄代表长生命周期的对象。对这些句柄的异步操作用**请求**（request）来标识。请求是短生命周期的（通常只在一次回调中使用），通常对应句柄上的一次 I/O 操作。请求用于在单个动作的发起与回调之间保存上下文。举例来说，一个 UDP 套接字由 `uv_udp_t` 表示，而对该套接字的每次单独写入则使用 `uv_udp_send_t` 结构，写入完成后该结构会被传递给回调。

句柄通过对应的

```c
uv_TYPE_init(uv_loop_t *, uv_TYPE_t *)
```

函数来建立。

回调是 libuv 在 watcher 感兴趣的事件发生时调用的函数。应用程序特有的逻辑通常就实现在回调里。例如，I/O watcher 的回调会收到从文件中读到的数据，定时器回调在超时时被触发，等等。

### Idling

下面是一个使用 idle 句柄的例子。其回调在事件循环的每一轮都会被调用一次。idle 句柄的一个用例将在 utilities 一章中讨论。让我们用 idle watcher 来观察 watcher 的生命周期，并看看当存在 watcher 时 `uv_run()` 会如何阻塞。计数达到目标后 idle watcher 被停止，由于不再有任何处于活动状态的事件 watcher，`uv_run()` 随之退出。

**idle-basic/main.c**

```c
#include <stdio.h>
#include <uv.h>

int64_t counter = 0;

void wait_for_a_while(uv_idle_t* handle) {
    counter++;

    if (counter >= 10e6)
        uv_idle_stop(handle);
}

int main() {
    uv_idle_t idler;

    uv_idle_init(uv_default_loop(), &idler);
    uv_idle_start(&idler, wait_for_a_while);

    printf("Idling...\n");
    uv_run(uv_default_loop(), UV_RUN_DEFAULT);

    uv_loop_close(uv_default_loop());
    return 0;
}
```

### 保存上下文

在基于回调的编程风格中，你常常需要在调用点与回调之间传递一些"上下文"——即应用特有的信息。所有句柄和请求都有一个 `void* data` 成员，你可以把它设置为上下文，并在回调中转换回原来的类型。这是 C 库生态中随处可见的常用模式。此外，`uv_loop_t` 也有一个类似的 data 成员。

---

> [^1]: 当然，这取决于硬件的性能。
